name = "vocal-edge-ai"
main = "src/worker/index.ts" # Ensure this path is correct
compatibility_date = "2024-10-01"

[assets]
directory = "./dist" # Where Vite builds your React files
binding = "ASSETS"   # This allows your script to call env.ASSETS.fetch()